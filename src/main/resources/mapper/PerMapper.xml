<?xml version="1.0" encoding="UTF-8"?>
	<!DOCTYPE mapper
			PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.ln.mapper.PerMapper">
			<select id="findPer" resultType="PermissionView">
				select * from t_permisssion
			</select>
	</mapper>